{
  "name": "@jev/web",
  "version": "0.1.0",
  "private": true,
  "description": "Promesómetro web: el acta de concreción del discurso político",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@jev/rhetoric": "workspace:*",
    "@preact/signals": "^2.0.0",
    "preact": "^10.26.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.0",
    "vite": "^6.3.0"
  }
}
